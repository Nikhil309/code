public class Dml1 {
    public void Ins(){
       list<Account> acclist=new list<Account>();
       for(Interger i=0;i<=acc.size(100);i++){
           Account acc=new Account(Name='Nikhil1'+i);
           acclist.add(acc);
       }
    insert acclist;
}